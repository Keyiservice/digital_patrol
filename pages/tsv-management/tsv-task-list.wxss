/* 全局设定 */
page {
  background-color: #f5f5f5;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
}
.container {
  padding-bottom: 180rpx;
}

/* 标题 */
.page-title {
  text-align: center;
  margin: 20rpx 0;
}
.title-text {
  display: block;
  font-size: 55rpx;
  font-weight: bold;
  color: #0d3371;
}
.sub-title-text {
  display: block;
  font-size: 38rpx;
  color: #0d3371;
  margin-top: 10rpx;
}

/* 记录列表 */
.records-scroll {
  width: 100%;
  height: calc(100vh - 480rpx);
  padding-bottom: 50rpx; /* 增加一个底部内边距，防止内容被遮挡 */
}
.record-card {
  background-color: #fff;
  border-radius: 10rpx;
  margin: 20rpx;
  padding: 20rpx;
  box-shadow: 0 2px 5px rgba(0,0,0,0.1);
}
.card-header {
  display: flex;
  justify-content: space-between;
  border-bottom: 1px solid #f0f0f0;
  padding-bottom: 15rpx;
  margin-bottom: 15rpx;
}
.project-text {
  font-weight: bold;
  font-size: 32rpx;
}
.date-text {
  font-size: 26rpx;
  color: #888;
}
.card-body .detail-label {
  font-size: 28rpx;
  color: #333;
  margin-bottom: 10rpx;
  display: block;
}
.card-footer {
  display: flex;
  justify-content: flex-end;
  margin-top: 25rpx;
  padding-top: 15rpx;
  border-top: 1px solid #f0f0f0;
}
.card-btn {
  background-color: #eee;
  font-size: 28rpx;
  padding: 15rpx 30rpx;
  margin-left: 15rpx;
  border-radius: 8rpx;
  line-height: 1.2;
  min-height: 70rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}
.start-btn {
  background-color: #0d3371;
  color: white;
  font-weight: bold;
  box-shadow: 0 2px 4px rgba(13, 51, 113, 0.3);
}
.no-records {
  text-align: center;
  color: #999;
  padding-top: 100rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
}

/* 加载中样式 */
.loading-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 300rpx;
  padding: 50rpx 0;
}

.loading-icon {
  width: 80rpx;
  height: 80rpx;
  border: 6rpx solid #f3f3f3;
  border-top: 6rpx solid #0d3371;
  border-radius: 50%;
  animation: spin 1s linear infinite;
  margin-bottom: 30rpx;
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

.loading-text {
  font-size: 30rpx;
  color: #666;
}
.congrats-text {
  font-size: 60rpx;
  font-weight: bold;
  color: #0d3371;
  margin-bottom: 30rpx;
  text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
  letter-spacing: 2rpx;
  font-family: 'Arial', sans-serif;
  transform: scale(1.1, 1);
}
.completion-text {
  font-size: 32rpx;
  color: #666;
  margin-top: 10rpx;
}

/* 底部按钮 */
.actions {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  display: flex;
  justify-content: space-around;
  padding: 20rpx;
  background-color: #f7f7f7;
  border-top: 1px solid #ddd;
}
.action-button {
  width: 45%;
  height: 80rpx;
  font-size: 32rpx;
  border-radius: 15rpx;
  display: flex;
  justify-content: center;
  align-items: center;
}
.actions .history-btn {
  background-color: #0d3371;
  color: white;
}
.actions .back-btn {
  background-color: #f0f0f0;
  color: #333;
}
